image: registry.gitlab.com/turtleopedia/turtleopedia:latest

variables:
  DEV_STAGE_NAME: dev
  PROD_STAGE_NAME: prod
  STAGING_STAGE_NAME: staging

  SERVER_CI_REGISTRY_IMAGE: registry.gitlab.com/turtleopedia/turtleopedia

stages:
  - build
  - test
  - package
  - deploy

# For manual triggering Step
.manual_step: &manual_step
  when: manual
  allow_failure: false

include:
  - local: '/deployments/gitlab-ci-configs/server.yml'
