# specify the node base image with your desired version node:<version>
FROM node:13

# Exposing our application's default port
EXPOSE 3000

WORKDIR /opt/api_server

ADD ./package.json /opt/api_server/package.json
